package BASICS_OF_JAVA;

// Here we are not taking radius input from user.

public class AreaOfCircle {
    public static void main(String[] args){
        double r = 4.0;
        double area = 3.14*r*r;
        System.out.println("Area Of Circle is:" + area); // Here we use the + to concatenate both of them simply we can
        // say to join them into single output.
    }
}
